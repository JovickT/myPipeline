services:
  frontend:
    build: ./frontend
    ports:
      - "3000:80"  # On mappe le port 3000 de ton PC vers le 80 du conteneur
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"